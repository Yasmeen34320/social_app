import 'package:flutter/material.dart';

class SharedContainer extends StatefulWidget {
  const SharedContainer({super.key});

  @override
  State<SharedContainer> createState() => _SharedContainerState();
}

class _SharedContainerState extends State<SharedContainer> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
